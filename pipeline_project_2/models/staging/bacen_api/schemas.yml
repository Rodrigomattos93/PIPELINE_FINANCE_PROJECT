version: 2

models:
  - name: stg_bacen__ipcas
    description: "Camada inicial com dados da fonte externa public.ipca"
    columns:
          - name: id
            description: Identificador único para cada registro do índice IPCA.
          - name: ipca_value
            description: Valor da variação mensal do índice IPCA na data correspondente.
          - name: date
            description: Data do registro do índice IPCA, convertida para o tipo `date`.
