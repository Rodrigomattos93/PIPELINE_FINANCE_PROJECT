name: 'pipeline_project_2'
version: '1.0.0'

profile: 'pipeline_project_2'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:         
  - "target"
  - "dbt_packages"

models:
  pipeline_project_2:
    staging:
      +materialized: view
    marts:
      +materialized: table
